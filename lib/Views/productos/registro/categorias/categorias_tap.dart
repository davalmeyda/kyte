import 'package:flutter/material.dart';

class CategoriasTab extends StatelessWidget {
  const CategoriasTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}